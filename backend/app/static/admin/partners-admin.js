async function loadApps(){const rows=await fetch('/seller-applications').then(r=>r.json());document.getElementById('appsWrap').innerHTML=`<table><thead><tr><th>ID</th><th>Status</th><th>Lokal</th><th>Kontakt</th><th>Adresa</th><th>Akcije</th></tr></thead><tbody>${rows.map(a=>`<tr><td>${a.id}<br><small>${a.created_at||''}</small></td><td><span class="status">${a.status}</span></td><td><strong>${a.business_name}</strong><br>${a.category||''}</td><td>${a.contact_name||''}<br>${a.phone||''}<br>${a.email||''}</td><td>${a.city||''}<br>${a.address||''}</td><td class="actions"><button onclick="approveApp('${a.id}')">Odobri</button><button class="secondary" onclick="setApp('${a.id}','contacted')">Kontaktiran</button><button class="danger" onclick="setApp('${a.id}','rejected')">Odbij</button>${a.store_id?`<br><small>Store #${a.store_id}, PIN ${a.seller_pin}</small>`:''}</td></tr>`).join('')}</tbody></table>`;}
async function setApp(id,status){await fetch(`/seller-applications/${id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status})});loadApps()}
async function approveApp(id){const r=await fetch(`/seller-applications/${id}/approve`,{method:'POST'});const data=await r.json();alert(data.ok?`Odobreno. Store ID: ${data.store_id}, PIN: ${data.seller_pin}`:JSON.stringify(data));loadApps()}
document.getElementById('loadAppsBtn').onclick=loadApps;loadApps();