document.getElementById('usePartnerGps')?.addEventListener('click',()=>{if(!navigator.geolocation){alert('GPS nije podržan');return;}navigator.geolocation.getCurrentPosition(p=>{latInput.value=p.coords.latitude;lngInput.value=p.coords.longitude;},e=>alert('GPS greška: '+e.message),{enableHighAccuracy:true,timeout:15000})});
document.getElementById('partnerForm')?.addEventListener('submit',async e=>{e.preventDefault();const fd=new FormData(e.target);const body=Object.fromEntries(fd.entries());['latitude','longitude'].forEach(k=>{body[k]=body[k]?Number(body[k]):null});const btn=e.target.querySelector('button[type="submit"]');btn.disabled=true;btn.textContent='Šaljem...';try{const r=await fetch('/seller-applications',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});if(!r.ok)throw new Error(await r.text());const data=await r.json();document.getElementById('partnerResult').textContent='Prijava poslata. ID: '+data.application.id;e.target.reset();}catch(err){document.getElementById('partnerResult').textContent='Greška: '+err.message}finally{btn.disabled=false;btn.textContent='Pošalji prijavu'}});