const $=id=>document.getElementById(id); function toast(m){const t=$('toast'); if(!t)return; t.textContent=m; t.classList.add('show'); setTimeout(()=>t.classList.remove('show'),2500)}
$('refundForm')?.addEventListener('submit',async(e)=>{e.preventDefault(); try{ const r=await fetch('/order-workflow/refund-requests',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({reservation_code:$('code').value,phone:$('phone').value,reason:$('reason').value})}); const d=await r.json(); if(!r.ok) throw new Error(d.detail||JSON.stringify(d)); $('result').textContent=JSON.stringify(d,null,2); toast('Zahtev poslat'); }catch(err){toast(err.message)}});
